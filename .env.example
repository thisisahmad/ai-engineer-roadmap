# Canonical origin used for Open Graph tags, sitemap.xml and robots.txt.
# Set this in the Vercel project once you have a production domain.
# Without it, builds fall back to the Vercel deployment host, then to localhost.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
